<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aminbadh.tdrprofessorslpm">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:configChanges="locale|orientation"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheDigitalRegister">
        <activity
            android:name=".activity.SettingActivity"
            android:parentActivityName=".activity.MainActivity" />
        <activity
            android:name=".activity.RecentAbsenceActivity"
            android:parentActivityName=".activity.RecentActivity" />
        <activity
            android:name=".activity.RecentActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".activity.ClassActivity" />
        <activity
            android:name=".activity.AbsenceActivity"
            android:parentActivityName=".activity.ClassActivity" />
        <activity
            android:name=".activity.ClassActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".activity.MainActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>